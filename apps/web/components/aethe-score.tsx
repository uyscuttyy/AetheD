export function AetheScore({ score, confidence, dimensions, compact = false }: { score: number; confidence: number; dimensions?: Record<string, number | null>; compact?: boolean }) {
  return <details className={`score ${compact ? "compact" : ""}`}><summary><span className="scoreNumber">{score}</span><span><strong>AetheScore</strong><small><i /> VERIFIED · {Math.round(confidence * 100)}% confidence</small></span></summary>{dimensions && <div className="scoreDetails"><strong>Why {score}?</strong>{Object.entries(dimensions).map(([name, value]) => <div className="metric" key={name}><span>{name}</span><span className="bar"><b style={{ width: `${value ?? 12}%` }} /></span><em>{value ?? "Unknown"}</em></div>)}<p>Unknown evidence is not treated as verified or silently scored.</p></div>}</details>;
}
