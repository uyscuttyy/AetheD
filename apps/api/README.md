# AetheD API

The framework-neutral API contract lives in `packages/domain/src/api.ts`. The web app currently mounts local development routes for submission and verification lookup under `apps/web/app/api/v1`. A dedicated durable API server remains planned.
